<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.CategoryMapper">
    <!--菜品分页查询-->
    <select id="SelectByPage" resultType="com.sky.entity.Category">
        select *from category
        <where>
            <if test="name!=null and name!=''">and name like concat('%',#{name},'%')</if>
            <if test="type!=null">and type=#{type}</if>
        </where>
        order by sort
    </select>

    <!--新增分类-->
    <insert id="addCategory">
        insert into category (id,name,type,sort,create_time,update_time,create_user,update_user,status)
        values (#{id},#{name},#{type},#{sort},#{createTime},#{updateTime},#{createUser},#{updateUser},#{status})
    </insert>
</mapper>
